1.可视化方案旨在解答什么问题？

旨在将北京地铁网站上散落的地图、站点首末班车时间、线路站间距等信息综合起来，把“查表/对照网页”的成本转化为“围绕地图的直观交互”。核心想解决的问题包括：

（1）站点/线路快速定位：在地图上直接搜索站点名称并定位、闪烁提示。
（2）线路对比与可读性：通过线路显隐控制与高亮策略，减少信息拥挤，便于只看关注的线路。
（3）路线结构与相邻站距离：点击线路后展示该线路的站点序列和相邻站间距，并给出总里程。
（4）时刻信息查询：在站点上悬停/点击查看首末班车信息；并支持按指定时间筛选“此时是否可能有车”的站点可见性。
（5）探索与浏览：支持拖拽平移、缩放，以及移动端常见的手势（平移/双击缩放/双指缩放）。

2.设计决策的依据。如何选择特定的可视化编码与交互技术？考虑过哪些替代方案？选择最终可视化的依据是什么？

将交互集中于地图本身，以“地图即入口”的方式组织功能：

（1）可视化编码
1）使用 SVG 作为渲染载体：地铁线路、站点、站名都绘制成矢量元素，缩放时保持清晰；同时 SVG 元素天然支持事件代理（线路点击、站点悬停）。
2）颜色编码：线路使用原数据提供的线路色（lc），并在控件中用同色标识，便于快速建立视觉对应关系。
3）分层绘制：将背景标识、线路、站点分别绘制到不同的 <g> 图层中，避免高亮线路时遮挡站点与换乘标识。

（2）交互设计
1）线路显隐：以勾选框控制线路的显示/弱化（dim），降低地图拥挤。
2）高亮模式切换：提供“取消高亮 / 悬停高亮 / 选中高亮”三种模式，兼顾探索（hover）与对比（selected）。
3）站点搜索：输入站名后联想提示，回车或选择后自动定位到地图对应站名并做短暂闪烁提示，减少在复杂地图中肉眼寻找的成本。
4）站间距展示：开启开关后，点击线路弹出底部面板，用横向时间轴/流程图的形式展示站点顺序与相邻站间距，并统计总距离。
5）首末班车提示：开启开关后，站点悬停/点击弹出 tooltip，显示该站点在不同线路/方向的首末班信息。
6）按时间筛选：通过小时/分钟选择器指定时间，依据首末班时间粗略判断站点是否“可能有车”，并用显示/隐藏站点与站名实现过滤；提供自动播放（每秒+5分钟）用于观察时段变化。

（3）数据组织与关联方式
1）地图与站点坐标来自 XML（beijing.xml），站点以属性 lb（站名）作为核心键。
2）站间距来自 JSON（spacings.json），按线路名称与站名建立索引。
3）首末班车来自 stations.xml，同样通过站名作为键映射到地图站点。

（4）替代方案与取舍
1）替代方案：使用静态图片地图 + 叠加其他信息；或使用 Canvas/WebGL 自绘。
2）最终选择依据：现有数据源本身是结构化的 XML，且包含坐标与线路结构，用 SVG 直接绘制可以最大化复用数据；svg-pan-zoom 等库可以快速获得成熟的缩放/平移体验；同时 SVG 对文字/图标与交互事件的支持更直接，开发复杂度更低。

3.外部资源引用。

（1）数据源
1）地图结构与坐标：来自北京地铁网站下载的 XML 地图数据（beijing.xml）。
2）站点首末班车：来自北京地铁网站的站点通车/时刻相关信息（stations.xml）。
3）站间距信息：由人工收集整理为 JSON（spacings.json）。

（2）第三方库
1）jQuery：用于 AJAX 加载 XML/JSON、DOM 操作与事件绑定。
2）svg-pan-zoom：用于 SVG 的缩放、平移、视图控制。
3）Hammer.js：用于移动端手势（平移、双击、双指缩放）并与 svg-pan-zoom 的自定义事件处理接口对接。

（3）其他资源
换乘标识图标 turn.png（在地图中用 image 标注换乘站）。

4.开发流程概述。包含对开发过程的评述，包括回答以下问题： 开发应用程序耗时约多少（以工时计）？哪些环节耗时最多？
1）绘制地图，并验证交互逻辑
2）添加其他控件，鼠标选中逻辑
3）添加站间距信息
4）添加时间信息以及相关交互
5）部署即验证
耗时约4天，其中绘制地图与部署验证耗时较长，主要在解决 bug 与交互细节。

补充说明（耗时点/难点）：
（1）XML 解析与绘制：需要把线路（直线/弧线）、站点坐标、站名、换乘标识等信息正确映射为 SVG 元素，并处理环线（loop）等特殊结构。
（2）交互层级与遮挡：高亮线路时容易遮挡站点，需要通过分层 <g> 或把高亮线路提升到顶层来保证可读性。
（3）站间距顺序恢复：站间距数据是“站点-站点邻接”的图结构，展示时需要尽量恢复成符合线路行进方向的站点序列，并对环线/缺失数据做回退策略。
（4）首末班车解析：站点首末班字段格式不完全规整，需要做字符串解析，并考虑跨午夜的情况（末班时间小于首班时间）。
（5）移动端缩放手势：需要把 Hammer 的手势事件转换为 svg-pan-zoom 可理解的 pan/zoom 行为。
（6）部署验证：由于数据文件通过 AJAX 加载，直接 file:// 打开可能受到浏览器安全策略限制，因此需要在本地/线上以静态服务器方式访问并验证。
